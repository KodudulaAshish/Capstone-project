import 'package:flutter/material.dart';

class MyIcons {
  var done = const Icon(
    Icons.done,
    color: Colors.green,
  );
  var unDo = const Icon(
    Icons.unpublished_rounded,
    color: Colors.red,
  );
  var list = const Icon(
    Icons.format_list_bulleted,
    color: Colors.blue,
  );
  var add = const Icon(
    Icons.thumb_up,
    color: Color(0xE8ECB95A),
  );
  var important = const Icon(
    Icons.priority_high_outlined,
    color: Colors.red,
  );
}
